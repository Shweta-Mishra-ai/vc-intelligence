{
  "name": "update_cron_job",
  "description": "Update an existing cron job in your Render account.",
  "arguments": {
    "type": "object",
    "properties": {
      "serviceId": {
        "description": "The ID of the service to update",
        "type": "string"
      }
    },
    "required": [
      "serviceId"
    ]
  }
}